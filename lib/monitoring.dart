import 'package:flutter/material.dart';

class MonitoringScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Monitoring Screen"),
      ),
      body: Center(
        child: Text("Welcome to the Monitoring Screen!"),
      ),
    );
  }
}
